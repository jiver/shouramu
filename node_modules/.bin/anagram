#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../anagram/bin/anagram" "$@"
  ret=$?
else 
  node  "$basedir/../anagram/bin/anagram" "$@"
  ret=$?
fi
exit $ret
